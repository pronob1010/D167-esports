# Core framework - project was built on the Django 3.2 LTS line.
Django>=3.2,<3.3

# Third-party packages used by the project (see INSTALLED_APPS in settings.py).
django-jazzmin>=2.5,<3.0        # admin theme ('jazzmin')
django-smart-selects>=1.5,<2.0  # chained/grouped foreign keys ('smart_selects')
django-crispy-forms>=1.13,<2.0  # form rendering ('crispy_forms', bootstrap pack)

# Required for Django ImageField uploads (team/player/match images).
Pillow>=9.0

# Loads variables from a local .env file in development (optional at runtime).
python-dotenv>=1.0

# HTTP client for the bKash payment gateway (Stage D).
requests>=2.28
